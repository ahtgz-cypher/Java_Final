═══════════════════════════════════════════════════════════════
📋 HƯỚNG DẪN CẤU TRÚC DỰ ÁN - QUẢN LÝ ĐIỂM SINH VIÊN
═══════════════════════════════════════════════════════════════

⚠️ QUAN TRỌNG: ĐỌC KỸ TRƯỚC KHI BẮT ĐẦU LÀM VIỆC!

═══════════════════════════════════════════════════════════════
📁 CẤU TRÚC THƯ MỤC BẮT BUỘC:
═══════════════════════════════════════════════════════════════

Java_Final/
├── README.md                          # Hướng dẫn dự án
├── .gitignore                         # Đã có sẵn - KHÔNG XÓA
├── lib/                               # Thư viện MySQL
│   └── mysql-connector-j-9.5.0.jar   # Đã có sẵn
│
└── src/                               # Source code chính
    ├── Main.java                      # Entry point (đã có)
    │
    ├── config/                        # ⚠️ KHÔNG ĐƯỢC SỬA
    │   └── DBConnection.java          # Kết nối database (đã có)
    │
    ├── ui/                            # Giao diện - MỖI NGƯỜI LÀM FILE RIÊNG
    │   ├── LoginFrame.java           # Form đăng nhập (người làm auth)
    │   ├── RegisterFrame.java        # Form đăng ký (người làm auth)
    │   ├── AdminMainFrame.java       # Trang admin (người làm admin)
    │   ├── StudentMainFrame.java     # Trang sinh viên (người làm student)
    │   └── TeacherMainFrame.java     # Trang giáo viên (người làm teacher)
    │
    └── database/                      # Scripts SQL (đã có)
        ├── schema.sql                 # Tạo bảng
        ├── data.sql                   # Dữ liệu mẫu
        └── queries.sql                # Câu truy vấn mẫu

═══════════════════════════════════════════════════════════════
✅ QUY TẮC BẮT BUỘC KHI VIẾT CODE:
═══════════════════════════════════════════════════════════════

1. PACKAGE DECLARATION (BẮT BUỘC):
   ────────────────────────────────────────────────────────────
   ✅ ĐÚNG:
   - File trong src/ui/LoginFrame.java → package ui;
   - File trong src/config/DBConnection.java → package config;
   
   ❌ SAI:
   - Quên package declaration
   - Package không khớp với thư mục

2. IMPORT STATEMENTS:
   ────────────────────────────────────────────────────────────
   ✅ ĐÚNG:
   import config.DBConnection;        // Import từ package config
   import java.sql.Connection;
   import javax.swing.*;
   
   ❌ SAI:
   import DBConnection;                // Thiếu package name

3. CLASS NAMING:
   ────────────────────────────────────────────────────────────
   ✅ ĐÚNG: PascalCase
   - AdminMainFrame
   - LoginFrame
   - StudentMainFrame
   
   ❌ SAI:
   - adminMainFrame (chữ đầu không viết hoa)
   - admin_main_frame (dùng underscore)

4. METHOD NAMING:
   ────────────────────────────────────────────────────────────
   ✅ ĐÚNG: camelCase
   - addStudent()
   - getStudentById()
   - validateInput()
   
   ❌ SAI:
   - AddStudent() (viết hoa chữ đầu)
   - add_student() (dùng underscore)

═══════════════════════════════════════════════════════════════
📝 VÍ DỤ CODE CHUẨN:
═══════════════════════════════════════════════════════════════

VÍ DỤ 1: LoginFrame.java (người làm auth)
────────────────────────────────────────────────────────────
package ui;

import config.DBConnection;
import java.sql.Connection;
import javax.swing.*;

public class LoginFrame extends JFrame {
    // Code của bạn ở đây
}

VÍ DỤ 2: AdminMainFrame.java (người làm admin)
────────────────────────────────────────────────────────────
package ui;

import config.DBConnection;
import java.sql.Connection;
import javax.swing.*;
import java.sql.PreparedStatement;

public class AdminMainFrame extends JFrame {
    
    // Kết nối database
    private void addStudent(String name, String code) {
        try (Connection conn = DBConnection.getConnection()) {
            String sql = "INSERT INTO students (full_name, student_code) VALUES (?, ?)";
            PreparedStatement stmt = conn.prepareStatement(sql);
            stmt.setString(1, name);
            stmt.setString(2, code);
            stmt.executeUpdate();
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}

═══════════════════════════════════════════════════════════════
🔧 HƯỚNG DẪN SỬ DỤNG DATABASE:
═══════════════════════════════════════════════════════════════

1. KẾT NỐI DATABASE:
   ────────────────────────────────────────────────────────────
   Connection conn = DBConnection.getConnection();
   
   ⚠️ LUÔN DÙNG try-with-resources để tự động đóng connection:
   try (Connection conn = DBConnection.getConnection()) {
       // Code của bạn
   } catch (Exception e) {
       e.printStackTrace();
   }

2. CÁC BẢNG CÓ SẴN (xem schema.sql):
   ────────────────────────────────────────────────────────────
   - users (user_id, username, password, role_id)
   - roles (role_id, role_name)
   - students (student_id, user_id, full_name, student_code, dob)
   - teachers (teacher_id, user_id, full_name)
   - subjects (subject_id, subject_name, teacher_id, credit)
   - scores (score_id, student_id, subject_id, score)

3. ROLE_ID:
   ────────────────────────────────────────────────────────────
   - 1 = ADMIN
   - 2 = TEACHER  
   - 3 = STUDENT

═══════════════════════════════════════════════════════════════
🔗 LUỒNG HOẠT ĐỘNG CỦA ỨNG DỤNG:
═══════════════════════════════════════════════════════════════

1. Main.java khởi động
   └──> Hiển thị LoginFrame

2. LoginFrame (người làm auth)
   └──> Kiểm tra username/password trong bảng users
   └──> Nếu đúng:
        ├── role_id = 1 → Mở AdminMainFrame
        ├── role_id = 2 → Mở TeacherMainFrame
        └── role_id = 3 → Mở StudentMainFrame

3. RegisterFrame (người làm auth)
   └──> Thêm user mới vào bảng users
   └──> Mặc định role_id = 3 (STUDENT)

4. AdminMainFrame (người làm admin)
   └──> Thêm/xem sinh viên → bảng students
   └──> Thêm/xem giáo viên → bảng teachers
   └──> Thêm/xem môn học → bảng subjects

5. TeacherMainFrame (người làm teacher)
   └──> Xem danh sách sinh viên
   └──> Nhập điểm → bảng scores

6. StudentMainFrame (người làm student)
   └──> Xem điểm của mình từ bảng scores

═══════════════════════════════════════════════════════════════
🌿 QUY TRÌNH LÀM VIỆC VỚI GIT:
═══════════════════════════════════════════════════════════════

BƯỚC 1: Clone repository
────────────────────────────────────────────────────────────
git clone <url-repository>
cd Java_Final

BƯỚC 2: Tạo branch riêng (BẮT BUỘC)
────────────────────────────────────────────────────────────
git checkout -b feature/admin        # Người làm admin
git checkout -b feature/auth         # Người làm auth
git checkout -b feature/student      # Người làm student
git checkout -b feature/teacher     # Người làm teacher

BƯỚC 3: Làm việc trên branch của mình
────────────────────────────────────────────────────────────
# Chỉnh sửa code trong branch của bạn
# Commit thường xuyên

BƯỚC 4: Commit code
────────────────────────────────────────────────────────────
git add .
git commit -m "feat: Thêm chức năng đăng nhập"
git commit -m "fix: Sửa lỗi validation"
git commit -m "refactor: Tối ưu code"

⚠️ QUAN TRỌNG: Commit message phải rõ ràng!

BƯỚC 5: Push lên GitHub
────────────────────────────────────────────────────────────
git push origin feature/admin    # Thay bằng branch của bạn

BƯỚC 6: Tạo Pull Request trên GitHub
────────────────────────────────────────────────────────────
- Vào GitHub → Pull Requests → New Pull Request
- Chọn branch của bạn → main
- Mô tả những gì đã làm
- Đợi người khác review


═══════════════════════════════════════════════════════════════
✅ CHECKLIST TRƯỚC KHI PUSH CODE:
═══════════════════════════════════════════════════════════════

[ ] Code compile không lỗi (javac không báo lỗi)
[ ] Đã test chức năng cơ bản
[ ] Có package declaration đúng
[ ] Import statements đầy đủ và đúng
[ ] Đã commit trên branch riêng (không phải main)
[ ] Commit message rõ ràng
[ ] Không có file nhạy cảm (password hardcode)
[ ] Không commit file .class, .iml
[ ] Đã test kết nối database
[ ] Code format đẹp, dễ đọc


═══════════════════════════════════════════════════════════════
💡 LƯU Ý CUỐI CÙNG:
═══════════════════════════════════════════════════════════════

- Mỗi người chỉ làm file của mình trong thư mục ui/
- KHÔNG sửa file config/DBConnection.java
- KHÔNG sửa file Main.java (trừ khi được yêu cầu)
- Code truy vấn database viết trực tiếp trong UI class được

═══════════════════════════════════════════════════════════════
